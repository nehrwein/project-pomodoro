{
  "name": "project-pomodoro",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend"
  },
  "devDependencies": {
    "babel-plugin-styled-components": "^2.0.2"
  },
  "engines": {
    "node": "16.x"
  }
}
